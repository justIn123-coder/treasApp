
import React from 'react';

// The badge logic is now handled visually inside the screen headers (e.g. CollectionScreen)
// to provide a cleaner "hidden but working" background experience.
export const NetworkStatus: React.FC<{ syncStatus?: any }> = () => {
  return null;
};
